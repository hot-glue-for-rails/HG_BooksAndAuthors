<%= turbo_stream.replace "book__#{@book.id}" do %>
  <%= render partial: 'line', locals: {book: @book  } %>
<% end %>

<%= turbo_stream.replace "flash_notices" do %>
  <%= render partial: "layouts/flash_notices" %>
  <% if @book.errors.any? %>
  <%= render partial: "errors", locals: {resource: @book} %>
  <% end %>
<% end %>
